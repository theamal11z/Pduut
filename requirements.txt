# Core runtime dependencies for PDUUT — PDF Data Unification and Understanding Tool
# Pinned versions compatible with Streamlit Community Cloud

pymupdf==1.24.9
opencv-python-headless==4.10.0.84
pytesseract==0.3.10
# Pillow must be <11 for streamlit==1.35.0; 10.2.0 has stable wheels on Py311.
Pillow==10.2.0
numpy==1.26.4
pandas==2.2.2
streamlit==1.35.0
PyYAML==6.0.1
markdown==3.6
